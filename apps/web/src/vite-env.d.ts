// Injected at build time by vite.config.ts's `define` (app version from package.json,
// git commit from `git rev-parse` or the GIT_SHA build arg — see that file).
declare const __APP_VERSION__: string;
declare const __GIT_COMMIT__: string;
